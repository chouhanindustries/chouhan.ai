# add-site-backend

Add a thin capture layer (Cloudflare Pages Functions + D1 + Turnstile) so waitlist and contact forms write to owned storage; keep the site static, rent email sending, and leave commerce to Crowd Supply
